# WeaveNet 织网穿透 frps 容器配置
# 挂载于 weave-frps 容器 /app/frps.toml

nodeName = "上海主节点"
agentToken = "agent_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
panelBaseURL = "http://weave-panel:8000"

bindAddr = "0.0.0.0"
bindPort = 7000
vhostHTTPPort = 80
vhostHTTPSPort = 443
